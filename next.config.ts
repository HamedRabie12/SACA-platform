import type { NextConfig } from "next";const isVercel = Boolean(process.env.VERCEL);const nextConfig: NextConfig = {  output: isVercel ? undefined : "standalone",  reactStrictMode: true,  poweredByHeader: false,  images: {    remotePatterns: [      { protocol: "https", hostname: "images.unsplash.com" },      { protocol: "https", hostname: "unpkg.com" },      { protocol: "https", hostname: "*.tile.openstreetmap.org" },      { protocol: "https", hostname: "nominatim.openstreetmap.org" },    ],  },  experimental: {    optimizePackageImports: ["lucide-react"],  },};export default nextConfig;