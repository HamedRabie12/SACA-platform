"use client";import {useEffect,useState} from 'react';import {AdminGuard} from '@/components/layout/admin-guard';
export default function AdminPrivacy(){const [items,setItems]=useState<any[]>([]);const load=()=>fetch('/api/admin/privacy',{cache:'no-store'}).then(r=>r.json()).then(j=>setItems(j.requests||[]));useEffect(()=>{load()},[]);const update=async(id:string,status:string)=>{await fetch('/api/admin/privacy',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({id,status})});load()};return <AdminGuard><main className="min-h-screen bg-[var(--offwhite)] p-6"><div className="mx-auto max-w-7xl"><h1 className="text-3xl font-black">طلبات الخصوصية</h1><div className="mt-8 grid gap-3">{items.map(i=><div key={i.id} className="rounded-2xl border bg-white p-5 flex items-center justify-between gap-4"><div><div className="font-bold">{i.type}</div><div className="text-xs text-slate-500">{i.status}</div><div className="mt-1 text-sm">{i.details||''}</div></div><button onClick={()=>update(i.id,'COMPLETED')} className="rounded-lg bg-emerald-800 px-4 py-2 text-xs font-bold text-white">إتمام</button></div>)}{items.length===0&&<div className="rounded-2xl border border-dashed p-6 text-slate-500">لا توجد طلبات.</div>}</div></div></main></AdminGuard>}
