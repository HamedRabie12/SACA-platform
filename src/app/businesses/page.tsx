import { db } from "@/lib/db";
export const dynamic = "force-dynamic";
export default async function BusinessesPage(){ const businesses = await db.business.findMany({where:{verificationStatus:"VERIFIED"},orderBy:{createdAt:"desc"},take:60}); return <main className="min-h-screen bg-[var(--brand-paper)] px-4 py-10 md:px-8"><div className="mx-auto max-w-6xl"><p className="text-xs font-bold uppercase tracking-[.24em] text-emerald-700">Sudanese Businesses</p><h1 className="mt-2 text-4xl font-black">دليل الأعمال السودانية</h1><div className="mt-8 grid gap-4 md:grid-cols-3">{businesses.length===0?<div className="surface-elevated rounded-2xl p-6 text-slate-600">لا توجد أعمال موثقة منشورة حالياً.</div>:businesses.map(b=><article key={b.id} className="surface-elevated rounded-2xl p-6"><h2 className="font-bold">{b.name}</h2><p className="mt-2 text-sm text-slate-500">{b.category}</p><p className="mt-3 text-sm text-slate-600">{b.description}</p></article>)}</div></div></main>}
