import { db } from '@/lib/db';
export const dynamic = "force-dynamic";
export default async function BoardPage(){const boards=await db.board.findMany({where:{status:'ACTIVE'},orderBy:{createdAt:'desc'},take:10});const members=await db.boardMember.findMany({where:{status:'ACTIVE'},orderBy:{createdAt:'asc'},take:100});return <main className="min-h-screen bg-slate-50 px-4 py-10 md:px-8"><div className="mx-auto max-w-6xl space-y-8"><header><p className="text-xs font-bold uppercase tracking-[.22em] text-emerald-700">Board</p><h1 className="mt-2 text-4xl font-black">مجلس الإدارة</h1><p className="mt-3 text-slate-600">سجل المجلس النشط والمناصب والفترات المعتمدة. لا تعرض المنصة منصباً ما لم يكن مسجلاً في مصدر حوكمي.</p></header><div className="grid gap-4 md:grid-cols-2">{boards.map(b=><div key={b.id} className="rounded-3xl border bg-white p-6 shadow-sm"><div className="font-black">{b.title}</div><div className="mt-2 text-sm text-slate-500">{b.startDate?b.startDate.toLocaleDateString('ar-EG'):'—'} — {b.endDate?b.endDate.toLocaleDateString('ar-EG'):'مفتوح'}</div></div>)}{boards.length===0&&<div className="rounded-2xl border border-dashed p-6 text-slate-500">لا يوجد مجلس منشور حالياً.</div>}</div><section><h2 className="text-2xl font-black">أعضاء المجلس المسجلون</h2><div className="mt-4 grid gap-3">{members.map(m=><div key={m.id} className="rounded-2xl border bg-white p-5"><div className="font-bold">عضو مجلس #{m.memberId}</div><div className="mt-1 text-xs text-slate-500">{m.positionId||'بدون منصب معرف'}</div></div>)}{members.length===0&&<div className="rounded-2xl border border-dashed p-6 text-slate-500">لا يوجد أعضاء مجلس منشورون.</div>}</div></section></div></main>}
