import { db } from '@/lib/db';
export const dynamic = "force-dynamic";
export default async function CommitteesPage(){const committees=await db.committee.findMany({where:{status:'ACTIVE'},orderBy:{nameEn:'asc'}});return <main className="min-h-screen bg-slate-50 px-4 py-10 md:px-8"><div className="mx-auto max-w-6xl"><header><p className="text-xs font-bold uppercase tracking-[.22em] text-emerald-700">Committees</p><h1 className="mt-2 text-4xl font-black">اللجان</h1><p className="mt-3 text-slate-600">اللجان النشطة واختصاصاتها كما تُسجل في نظام الحوكمة.</p></header><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{committees.map(c=><article key={c.id} className="rounded-3xl border bg-white p-6 shadow-sm"><h2 className="font-black">{c.nameAr}</h2><div className="mt-1 text-xs text-slate-400">{c.nameEn}</div><p className="mt-4 text-sm leading-6 text-slate-600">{c.description||'لا يوجد وصف منشور بعد.'}</p><div className="mt-4 text-xs font-bold text-emerald-700">النطاق: {c.scope}</div></article>)}{committees.length===0&&<div className="rounded-2xl border border-dashed p-6 text-slate-500">لا توجد لجان منشورة حالياً.</div>}</div></div></main>}
