import { db } from "@/lib/db";
export const dynamic = "force-dynamic";
export default async function JobsPage(){ const jobs = await db.job.findMany({where:{status:"PUBLISHED"},orderBy:{createdAt:"desc"},take:50}); return <main className="min-h-screen bg-[var(--brand-paper)] px-4 py-10 md:px-8"><div className="mx-auto max-w-6xl"><p className="text-xs font-bold uppercase tracking-[.24em] text-emerald-700">Community Jobs</p><h1 className="mt-2 text-4xl font-black">الوظائف والفرص</h1><div className="mt-8 grid gap-4 md:grid-cols-2">{jobs.length===0?<div className="surface-elevated rounded-2xl p-6 text-slate-600">لا توجد وظائف منشورة حالياً.</div>:jobs.map(j=><article key={j.id} className="surface-elevated rounded-2xl p-6"><h2 className="font-bold text-lg">{j.title}</h2><p className="mt-1 text-sm text-slate-500">{j.employer}{j.location?` · ${j.location}`:""}</p><p className="mt-3 text-sm leading-6 text-slate-600">{j.description}</p></article>)}</div></div></main>}
