import Link from 'next/link';
export const dynamic = "force-dynamic";
import { resolveMemberSession } from '@/lib/security/member-session';
import { db } from '@/lib/db';
export default async function PortalPage(){const session=await resolveMemberSession();if(!session)return <main className="min-h-screen grid place-items-center bg-slate-50 p-6"><div className="rounded-3xl border bg-white p-8 text-center"><h1 className="text-3xl font-black">بوابة العضو</h1><p className="mt-3 text-slate-600">سجّل الدخول للوصول إلى عضويتك وخدماتك وطلباتك.</p><Link href="/auth/login" className="mt-5 inline-flex rounded-xl bg-emerald-800 px-5 py-3 font-bold text-white">تسجيل الدخول</Link></div></main>;const [member,membership,requests,volunteer]=await Promise.all([db.member.findUnique({where:{id:session.memberId},select:{name:true,accountState:true,stateId:true,cityId:true}}),db.membership.findFirst({where:{memberId:session.memberId},orderBy:{createdAt:'desc'}}),db.serviceRequest.count({where:{memberId:session.memberId,status:{not:'CLOSED'}}}),db.volunteer.findUnique({where:{memberId:session.memberId}})]);return <main className="min-h-screen bg-slate-50 px-4 py-10 md:px-8"><div className="mx-auto max-w-7xl"><header><p className="text-xs font-bold uppercase tracking-[.22em] text-emerald-700">Member Portal</p><h1 className="mt-2 text-4xl font-black">مرحباً {member?.name||'بالعضو'}</h1><p className="mt-2 text-slate-600">مركزك الشخصي للمشاركة في الجالية والخدمات والحوكمة.</p></header><div className="mt-8 grid gap-4 md:grid-cols-4"><Card title="العضوية" value={membership?.status||'غير مفعلة'} href="/portal/membership"/><Card title="طلبات الخدمة" value={String(requests)} href="/portal/service-requests"/><Card title="التطوع" value={volunteer?'مسجل':'غير مسجل'} href="/portal/volunteer"/><Card title="الأمان والخصوصية" value="مركز التحكم" href="/portal/security"/></div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3"><Link href="/elections" className="rounded-3xl bg-slate-950 p-7 text-white"><div className="text-xs uppercase tracking-widest text-emerald-300">Governance</div><h2 className="mt-2 text-2xl font-black">الانتخابات والحوكمة</h2><p className="mt-2 text-sm text-white/70">تحقق من أهليتك وشارك عندما تكون الانتخابات مفتوحة.</p></Link><Link href="/meetings" className="rounded-3xl border bg-white p-7"><div className="text-xs uppercase tracking-widest text-emerald-700">Community</div><h2 className="mt-2 text-2xl font-black">الاجتماعات</h2><p className="mt-2 text-sm text-slate-600">الاجتماعات العامة والافتراضية والحضور.</p></Link><Link href="/governance" className="rounded-3xl border bg-white p-7"><div className="text-xs uppercase tracking-widest text-emerald-700">Trust</div><h2 className="mt-2 text-2xl font-black">الشفافية والحوكمة</h2><p className="mt-2 text-sm text-slate-600">الدستور والوضع القانوني والقرارات المنشورة.</p></Link></div></div></main>}
function Card({title,value,href}:{title:string;value:string;href:string}){return <Link href={href} className="rounded-3xl border bg-white p-6 shadow-sm hover:-translate-y-1 hover:shadow-lg"><div className="text-xs text-slate-500">{title}</div><div className="mt-2 text-2xl font-black text-slate-900">{value}</div></Link>}
